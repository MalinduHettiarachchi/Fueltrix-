import React from 'react';
import Navbar from '../CMNav/navbar';
import '../dashboard/dashboard.css';  // Make sure to import the CSS file

function Dashboard() {
  return (
    <div className="dashboard">
      <Navbar />
    </div>
  );
}

export default Dashboard;
